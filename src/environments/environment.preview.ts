// Preview / staging environment — used by `npm run build:preview`
export const environment = {
  production: true,
  apiBase: 'https://staging-api.amantalenthunt.com', // TODO: point to the real preview API
};
